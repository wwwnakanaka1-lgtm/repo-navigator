[project]
name = "repo-navigator-backend"
version = "0.1.0"
description = "FastAPI backend for Repo Navigator"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
